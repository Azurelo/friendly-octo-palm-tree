<div class="text-center col-12">
  <h2>{{name}}</h2>
  <div class="singlePet"><img src="/images/{{picture}}.jpg" alt="{{name}}" class="card-img-top petImage"></div>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Mythical Mutts are cool as heck lets go!!!!!!</button>
  </p>
</div>
